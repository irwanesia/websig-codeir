# Izinkan akses hanya ke offices.json

<FilesMatch "^offices\.json$">
Order allow,deny
Allow from all
<IfModule mod_authz_core.c>
Require all granted
</IfModule>
</FilesMatch>

# Blokir semua file lainnya di folder data

<FilesMatch "^.\*$">
Order allow,deny
Deny from all
<IfModule mod_authz_core.c>
Require all denied
</IfModule>
</FilesMatch>
<Files "offices.json">
Order allow,deny
Allow from all
<IfModule mod_authz_core.c>
Require all granted
</IfModule>
</Files>
